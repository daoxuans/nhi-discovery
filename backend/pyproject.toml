[project]
name = "ndpid-web-console"
version = "0.1.0"
description = "nDPId Web Console - Real-time traffic analysis with AI protocol detection"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "websockets>=12.0",
    "pydantic>=2.0",
]
